import './PetOrnaments.css';

export const PetOrnaments = () => {
  const pets = ['🐱', '🐶', '🐈', '🐕', '😺', '🐾'];

  return (
    <div className="pet-ornaments">
      {pets.map((pet, index) => (
        <div key={index} className="pet">
          {pet}
        </div>
      ))}
    </div>
  );
};
